import { Metadata } from "next";
import PageContent from "./content";

export const metadata: Metadata = {
  title: 'Live Stats',
};

const LiveStats = () => <PageContent />;

export default LiveStats;
