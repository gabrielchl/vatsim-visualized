import { Metadata } from "next";
import PageContent from "./content";

export const metadata: Metadata = {
  title: 'Historic Stats',
};

const HistoricStats = () => <PageContent />;

export default HistoricStats;